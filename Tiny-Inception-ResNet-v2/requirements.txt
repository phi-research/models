# Pinned to match the original publication-era environment.
# These imports (keras.engine.topology, keras_applications) were
# removed in modern TF/Keras — do not bump without porting the code.
tensorflow==1.15.5
keras==2.2.5
keras-applications==1.0.8
keras-preprocessing==1.1.2
h5py<3.0

# Scientific stack
numpy>=1.19,<1.24
pandas>=1.1
scikit-learn>=0.24
matplotlib>=3.3
pillow>=8.0

# Notebook environment
jupyter>=1.0
ipykernel>=6.0
